### USER QUESTION ###
{question}

---

# Task: Search Term Breakdown
1. Identify the query type (Grievance, Procedural, Eligibility, Valuation, Conditional, or Multiple Issues).
2. Generate 2 semantic search queries that will retrieve the most relevant legal content for the user's inquiry.

Return ONLY a JSON array of query strings, for example:
["query one", "query two"]